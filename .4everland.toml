# 4EVERLAND Configuration
# Force npm and Node 18

[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "18.17.0"
  NPM_VERSION = "9.6.7"
  PACKAGE_MANAGER = "npm"
