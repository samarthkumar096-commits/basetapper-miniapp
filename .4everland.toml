# 4EVERLAND Configuration
# This file tells 4EVERLAND how to build your project

# Use npm instead of yarn
[build]
  command = "npm install && npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"
